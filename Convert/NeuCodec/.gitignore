# Model weights — pulled from huggingface.co/neuphonic/neucodec on
# demand; not tracked here.
models/

# Conversion outputs — re-derivable from `python -m scripts.convert_to_tflite`.
output/*
!output/.gitkeep

# Python venv + caches
.venv/
__pycache__/
*.pyc
